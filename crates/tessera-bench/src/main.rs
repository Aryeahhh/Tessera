//! Tessera load generator and baseline-comparison harness — arrives in Layer 7.
//!
//! Placeholder entry point for the Layer 0 scaffold; the concurrency sweep and
//! throughput/TTFT measurement land later (see `CLAUDE.md` §3, Layer 7).
#![forbid(unsafe_code)]

fn main() {
    println!("tessera-bench: not yet implemented (Layer 7)");
}
