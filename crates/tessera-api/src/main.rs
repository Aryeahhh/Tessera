//! Tessera HTTP/SSE serving front end (axum) — arrives in Layer 6.
//!
//! Placeholder entry point for the Layer 0 scaffold; the server, token
//! streaming, and metrics endpoints are added later (see `CLAUDE.md` §3,
//! Layer 6).
#![forbid(unsafe_code)]

fn main() {
    println!("tessera-api: not yet implemented (Layer 6)");
}
