//! Repository automation entry point (fmt / lint / test orchestration).
//!
//! Optional helper, intentionally minimal in Layer 0; subcommands are added as
//! the workflow needs them.
#![forbid(unsafe_code)]

fn main() {
    println!("xtask: no automation subcommands defined yet");
}
